version: '3.8'

services:
  quantumshield:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./artifacts:/app/artifacts
      - ./backend/system_audit.jsonl:/app/backend/system_audit.jsonl
    restart: unless-stopped
